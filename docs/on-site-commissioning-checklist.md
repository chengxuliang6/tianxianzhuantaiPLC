# Easy521 转台现场联调清单

状态：待现场执行。本文是操作顺序和记录表，不代表任何现场项目已通过。

## 绝对停止条件

出现以下任一情况立即停止测试、切断伺服动力并排查：方向与指令不符、位置跳变、速度异常、异响/振动、机械干涉、线缆缠绕、报警、EtherCAT 掉站、软件停止未按预期减速、电脑断线后仍继续运动、人员进入旋转区域。

红色“停止（软件）”、空格键和通信心跳都不是实体急停。现场当前没有实体急停，因此只允许空载、人员远离、1°/s 的受控初次调试；不允许带载、整圈或更高速度测试。应明确一名操作员守在电脑和可切断伺服动力的位置。

## A. 不上电/不运动检查

- [ ] 转台空载，旋转范围清空，线缆留有余量，无机械干涉。
- [ ] PLC、伺服、减速器、齿轮与转台固定可靠；总减速比确认暂为 `5 × 10 = 50:1`。
- [ ] 电脑网线连接 PLC `EtherNET1`；Type-C 只用于 PLC 编程；EtherCAT 连接伺服 `CN3`。
- [ ] 操作员知道伺服主电源/动力切断方法，其他人员退出旋转区域。
- [ ] 记录 PLC 型号/固件、AutoShop 版本、伺服型号/固件、电脑网卡名称。

只读记录：

| 项目 | 现场值 | 结果/备注 |
|---|---|---|
| PLC IPv4 / 掩码 |  |  |
| 电脑 Ethernet IPv4 / 掩码 |  |  |
| EtherCAT 轴状态 |  |  |
| PLC 扫描周期/最大抖动 |  |  |
| AutoShop 编译错误/警告数 |  |  |

## B. AutoShop 离线编译门禁（本节禁止下载）

- [ ] 参考源码已按 `plc/README.md` 逐页录入并适配当前固件类型名。
- [ ] AutoShop 编译为 `0 error`、`0 warning`；结果已保存并记录。
- [ ] `D0:D19`、`D100:D120`、`D200:D206`、`D2000:D4159` 变量表绑定与数据类型逐项核对。
- [ ] `Axis_0` 单位、电子齿轮、编码器反馈和 50:1 缩放离线核对完成。
- [ ] 编译结果、警告解释和待适配项已保存；本节不下载程序、不使能轴、不写 PLC。

## C. 一次性下载授权与无运动下载

- [ ] A、B 两节均已完成；伺服保持禁用和静止，操作员可随时切断动力。
- [ ] 操作员一次性明确授权通过 Type-C 下载协议 v2 程序；授权只覆盖本次下载，不覆盖任何 Modbus 写入、伺服使能或运动。
- [ ] 下载期间电脑端不得连接 Modbus、不得产生任何 PC 写入、不得使能伺服或运动。
- [ ] 仅下载已离线编译为 `0 error`、`0 warning` 的协议 v2 程序，并记录下载时间与结果。

## D. 下载后只读网络与 Modbus 合同

下载完成后记录明确的 PLC IP；只检查这一张网卡和这一台 PLC，不扫描其他网段。

- [ ] 电脑与 PLC 在同一 IPv4 子网；ping/ARP 对应唯一目标。
- [ ] TCP 502 仅对已确认 PLC 地址做连通性检查。
- [ ] 下载完成并重新连接后，首先只读 `D200:D202`：`D200` 预期协议版本 `2`，`D201:D202` 预期两个 16 位字依次为 `0x1234, 0x5678`，组合为 `0x12345678`。这是 PLC 写入的只读门禁。
- [ ] 只读状态块 `D100:D120`，确认静态安全状态：轴静止、伺服未使能、无故障、实际速度近 0；任何未知枚举或无效反馈均停止。
- [ ] 明确记录方向编码：正转为 `+1`，反转=-1；本节不写入 PLC。
- [ ] PLC 重启后旧事件字无效：即使 `D2000:D4159` 原始字仍存在，也绝不得导出或确认。
- [ ] 以上下载后只读门禁全部通过前，禁止任何 PC Modbus 写入、伺服使能或运动。

记录：

| 寄存器/检查 | 预期 | 实测 | 结论 |
|---|---|---|---|
| D200 | 2 |  |  |
| D201 | 0x1234 |  |  |
| D202 | 0x5678 |  |  |
| D100 RUN_STATE | 安全静止状态 |  |  |
| D107:D108 速度 | 约 0°/s |  |  |

协议或字序不符时禁止 PC 写入和运动。修正程序后必须重新离线编译、重新取得一次性下载授权并重复 C、D 两节；不得临时在多个调用点交换高低字。

## E. 首次空载、1°/s、极小位移验证

本节必须由现场人员观察机械运动。首次不得直接自动一圈。

1. [ ] 伺服使能后保持静止，无报警、无爬行；实际速度接近 0。
2. [ ] 人工目视确认物理参考位置后设零，记录设零前后实际位置。
3. [ ] 选择手动、正转、1°/s；启动后观察实际位置，约 1°内立即按软件停止，确认平滑减速并记录最终位置。
4. [ ] 回到安全参考区后选择手动、反转=-1、1°/s，同样在约 1°内停止并记录。
5. [ ] 两次方向均正确、位置连续、停止距离可接受后，才测试拔除电脑—PLC 网线；只允许 1°/s 空载，确认超过 1 s 后受控停止且重连不自动恢复。
6. [ ] 检查 CSV 为严格 1～N；若在跨过第 1°前停止，应只有一条运行元数据记录，事件字段为空。

| 项目 | 指令 | 最终位置 | 停止距离/时间 | 结果/备注 |
|---|---|---:|---:|---|
| 正向极小位移 | 手动 CW 1°/s + STOP |  |  |  |
| 反向极小位移 | 手动 CCW 1°/s + STOP |  |  |  |
| 心跳断线 | 1°/s + 断网 |  |  |  |

## F. 本阶段明确禁止的测试

在安装并验证实体急停/安全回路之前，本项目不批准也不规划更高速度、带载或自动整圈现场测试。本清单不能作为提速授权；后续若安全硬件和风险评估发生变化，必须另行制定并审批新的测试方案。

## G. 现场结论

- [ ] AutoShop 实际编译通过。
- [ ] Modbus 字序和状态只读合同通过。
- [ ] 正反方向与 50:1 缩放通过。
- [ ] 软件停止与心跳停止通过（仍非安全等级）。
- [ ] 逐度数据与 CSV 通过。
- [ ] 现场限制、异常和原始日志已附到 `docs/verification-report.md`，没有把“待验证”改写成“已通过”。

执行人：__________  复核人：__________  日期：__________
